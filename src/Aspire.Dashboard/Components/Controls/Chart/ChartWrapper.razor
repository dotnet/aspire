@using Aspire.Dashboard.Model
@using Aspire.Dashboard.Otlp.Model

<div class="metric-tab">
    <PlotlyChart InstrumentViewModel="@InstrumentViewModel" Duration="Duration" Applications="Applications" IsDashpage="@IsDashpage"/>
    <ChartFilters InstrumentViewModel="@InstrumentViewModel" Instrument="@Instrument" DimensionFilters="@DimensionFilters" />
</div>

@code {
    [Parameter]
    public required List<OtlpApplication> Applications { get; set; }

    [Parameter, EditorRequired]
    public required TimeSpan Duration { get; set; }

    [Parameter, EditorRequired]
    public required List<DimensionFilterViewModel> DimensionFilters { get; set; }

    [Parameter, EditorRequired]
    public required OtlpInstrument Instrument { get; set; }

    [Parameter, EditorRequired]
    public required InstrumentViewModel InstrumentViewModel { get; set; }

    [Parameter]
    public bool IsDashpage { get; set; }
}
