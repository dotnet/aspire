{
  "definitions": {
    "logLevel": {
      "properties": {
        "Seq": {
          "$ref": "#/definitions/logLevelThreshold"
        }
      }
    }
  },
  "properties": {
    "Aspire": {
      "type": "object",
      "properties": {
        "Seq": {
          "type": "object",
          "properties": {
            "ApiKey": {
              "type": "string",
              "description": "A Seq API key that authenticates the client to the Seq server."
            },
            "HealthChecks": {
              "type": "boolean",
              "description": "Is the Seq server health check enabled."
            },
            "ServerUrl": {
              "type": "string",
              "description": "The base URL of the Seq server (including protocol and port). E.g. \"https://example.seq.com:6789\""
            }
          },
          "description": "Provides the client configuration settings for connecting telemetry to a Seq server."
        }
      }
    }
  },
  "type": "object"
}
