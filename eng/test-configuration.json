{
  "version": 2,
  "defaultOnFailure": "fail",
  "localRerunCount": 1,
  "remoteRerunCount": 2,
  "retryOnRules": [
    { "failureMessage" : { "regex" : "open.*docker.*GetImageBlob.*: no such file or directory" }
  ]
}
