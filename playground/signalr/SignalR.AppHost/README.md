# Aspire AppHost sample for Azure SignalR

## Usage

Update [appsettings.json](./appsettings.json] to use your `SubscriptionId`, `ResourceGroup` and `Location` to provision the Azure SignalR resource.
