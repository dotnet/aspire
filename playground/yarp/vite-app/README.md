# Vite App for YARP Playground

This is a simple Vite application that demonstrates the `AddYarpNpmApp` API.

## Development

To run locally:
```bash
npm install
npm run dev
```

## Build

To build for production:
```bash
npm run build
```

The output will be in the `dist` directory.
