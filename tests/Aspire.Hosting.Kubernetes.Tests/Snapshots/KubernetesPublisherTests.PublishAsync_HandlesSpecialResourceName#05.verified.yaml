---
apiVersion: "v1"
kind: "Service"
metadata:
  name: "special-app-service"
spec:
  type: "ClusterIP"
  selector:
    app: "aspire"
    component: "SpeciaL-ApP"
  ports:
    - name: "http"
      protocol: "TCP"
      port: 80
      targetPort: "http"
