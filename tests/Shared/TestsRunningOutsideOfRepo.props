<Project>
  <!-- FIXME: we don't have IsAspireHost property available here yet! -->
  <Import Project="$(RepoRoot)src/Aspire.Hosting.AppHost/build/Aspire.Hosting.AppHost.props"
          Condition="'$(TestsRunningOutsideOfRepo)' != 'true' and $(MSBuildProjectName.EndsWith('.AppHost'))" />
          <!--Condition="'$(TestsRunningOutsideOfRepo)' != 'true' and '$(IsAspireHost)' == 'true'" />-->
</Project>
